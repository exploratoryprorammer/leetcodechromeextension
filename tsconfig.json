{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "Node",
    "outDir": "chrome-extension",
    "strict": true,
    "skipLibCheck": true,
    "types": ["chrome"] // Only include default types, exclude problematic ones
  },
  "include": ["src/**/*.ts"]
}
